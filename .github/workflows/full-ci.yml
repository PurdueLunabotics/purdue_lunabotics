name: Continuous Integration Tests

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - master 
      - develop  
  pull_request:
    branches:
      - master 
      - develop  

jobs:
  build-container:
    name: Build the devcontainer
    runs-on: ubuntu-latest

    permissions:
      packages: write

    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          platforms: linux/amd64,linux/arm64
          build-args: |
            BUILDKIT_CONTEXT_KEEP_GIT_DIR=1
          push: true
          tags: |
            ghcr.io/purduelunabotics/development:${{ github.sha }}
            ghcr.io/purduelunabotics/development:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  run-build:
    name: Make sure the code builds
    needs: build-container
    runs-on: ubuntu-latest
    container: ghcr.io/purduelunabotics/development:${{ github.sha }}

    steps:
      - name: Test building
        shell: bash
        run: cd /luna_ws/ && source /opt/ros/jazzy/setup.bash && colcon build
